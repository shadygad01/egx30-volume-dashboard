GitHub verification on 2026-08-16: `gh api user` authenticated as shadygad01 (id 18125245). API confirms `shadygad01/egx30-volume-dashboard` exists, owner shadygad01, private visibility, default branch main, URL https://github.com/shadygad01/egx30-volume-dashboard. GitHub profile page opened without a browser login; it showed Sign in/Sign up and only public pinned repositories (aux, EGX-Genom, smartlist), so the private EGX30 repository is not visible on the logged-out public profile. The direct repository URL requires signing in to the shadygad01 account that owns the private repository.
